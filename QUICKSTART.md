# Быстрый старт 🚀

## Шаг 1: Настройка Supabase (5 минут)

1. Создайте аккаунт на [supabase.com](https://supabase.com) (если нет)
2. Создайте новый проект
3. В SQL Editor выполните миграции по порядку:
   - Откройте файл `supabase/migrations/001_initial_schema.sql` → скопируйте и выполните
   - Откройте файл `supabase/migrations/002_rls_policies.sql` → скопируйте и выполните  
   - Откройте файл `supabase/migrations/003_seed_data.sql` → скопируйте и выполните
4. В Database → Extensions включите расширение `vector`
5. В Settings → API скопируйте:
   - Project URL
   - `anon` key (публичный ключ)
   - `service_role` key (секретный ключ)

## Шаг 2: Настройка Backend (3 минуты)

```bash
# Перейдите в директорию backend
cd backend

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте его
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Создайте файл .env
touch .env  # или создайте вручную
```

Откройте `backend/.env` и заполните:

```env
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_KEY=ваш-anon-key
SUPABASE_SERVICE_KEY=ваш-service-key
DATABASE_URL=postgresql://postgres:пароль@db.supabase.co:5432/postgres

OPENAI_API_KEY=sk-ваш-openai-ключ
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

REDIS_URL=redis://localhost:6379/0

SECRET_KEY=любой-секретный-ключ-для-приложения
ENVIRONMENT=development
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

DEFAULT_SLA_ACCEPT_MINUTES=15
DEFAULT_SLA_REMOTE_MINUTES=60
```

**Важно:** Замените все значения на реальные из вашего Supabase проекта!

## Шаг 3: Запуск Backend

```bash
# Убедитесь, что вы в директории backend и venv активирован
uvicorn main:app --reload
```

Backend запустится на `http://localhost:8000`
Проверьте: откройте `http://localhost:8000/docs` - должна открыться документация API

## Шаг 4: Настройка Frontend (2 минуты)

Откройте **новый терминал**:

```bash
# Перейдите в директорию frontend
cd frontend

# Установите зависимости
npm install

# Создайте файл .env
touch .env  # или создайте вручную
```

Откройте `frontend/.env` и заполните:

```env
VITE_SUPABASE_URL=https://ваш-проект.supabase.co
VITE_SUPABASE_KEY=ваш-anon-key
VITE_API_URL=http://localhost:8000
```

## Шаг 5: Запуск Frontend

```bash
# Убедитесь, что вы в директории frontend
npm run dev
```

Frontend запустится на `http://localhost:3000`

## Шаг 6: Создание первого пользователя

1. Откройте Supabase Dashboard → Authentication → Users
2. Нажмите "Add user" → "Create new user"
3. Введите email и пароль
4. В SQL Editor выполните:

```sql
UPDATE public.users 
SET role = 'admin', name = 'Администратор'
WHERE email = 'ваш-email@example.com';
```

## Готово! ✅

1. Откройте `http://localhost:3000`
2. Войдите с созданным пользователем
3. Начните работу!

---

## Если что-то не работает:

### Backend не запускается
- Проверьте, что все зависимости установлены: `pip list`
- Проверьте `.env` файл - все переменные заполнены?
- Проверьте логи в терминале

### Frontend не запускается  
- Проверьте, что Node.js установлен: `node --version`
- Удалите `node_modules` и установите заново: `rm -rf node_modules && npm install`
- Проверьте `.env` файл

### Ошибки подключения к Supabase
- Проверьте URL и ключи в `.env`
- Убедитесь, что миграции выполнены
- Проверьте логи в Supabase Dashboard

### Ошибки OpenAI
- Проверьте баланс на [platform.openai.com](https://platform.openai.com)
- Убедитесь, что API ключ правильный

